# highlight-webpack-plugin

Use highlight.js with webpack.

~~~js
const Hwp = require('highlight-webpack-plugin')

webpackConfig.plugins.push(new Hwp);
~~~

for now can import styles from 'highlight.js/styles/'
